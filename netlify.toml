# Specify build settings
[build]
  # The directory to publish (the public directory)
  publish = "dist"
  # The build command
  command = "npm run build"

# Redirect all routes to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200