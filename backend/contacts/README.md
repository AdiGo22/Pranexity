# Pranexity — Contacts Backend

Simple TypeScript + Express backend for the Contacts section using Prisma + SQLite.

## Requirements
- Node 18+
- npm

## Setup
1. Copy `.env.example` to `.env`:
   ```bash
   cp .env.example .env
